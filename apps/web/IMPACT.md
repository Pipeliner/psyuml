# Impact — `apps/web/` (the GUI editor)

**Purpose:** the browser-based PsyUML editor. (M0: empty-canvas shell; M2: the drag-and-drop
editor over the 8-symbol Tier-1 core.)
**Status:** active (M2 — the editor is implemented: structured authoring, layers, links, snapshots/diff)
**Spec anchor / REQ:** REQ-EDITOR-MVP, REQ-DECISION-NAV, REQ-RESOURCE-ANCHOR, REQ-EXAMPLE-LIBRARY

## Upstream (this depends on)
- `react`, `react-dom`, `vite` (built from the repo root via `../../vite.config.ts`).
- `@psyuml/model`, `@psyuml/render`, `@psyuml/validate`, `@psyuml/profiles`, `@psyuml/diff`, `@psyuml/grammar` (live render + lint + cross-school vocabulary + longitudinal compare + text-DSL view/edit; M2/M3/M4/M6/M9). Editor state logic is in `editor.ts`.

## Downstream (depends on this) — blast radius
> **Blast radius: low.** A leaf app; nothing imports it. Built to `dist/web` (gitignored).

## Files
| File | Purpose | Upstream | Downstream | Spec / REQ | Change risk |
|---|---|---|---|---|---|
| `package.json` | Workspace member manifest (`web`) | — | workspace resolution | — | low |
| `index.html` | Vite entry; mounts `#root`, loads `main.tsx`; a minimal pre-bundle baseline (WCAG 2.4.7 visible focus + page background); the full design system lives in `styles.css` (ADR-0013); **`<head>` carries the title/description, `theme-color` (light+dark), favicons + apple-touch + web-app `manifest` (base-rewritten public paths), and absolute Open Graph + Twitter tags** (assets in `public/`, generated by `scripts/gen-icons.mjs`) | `main.tsx`, `public/*` | build | WCAG 2.4.7/2.5.8 | low |
| `main.tsx` | React bootstrap (mounts `App`; imports `styles.css`) | react-dom, `App.tsx`, `styles.css` | build | — | low |
| `styles.css` | **Hand-rolled, token-based design system (no UI framework; ADR-0013; identity ADR-0026).** **Visual identity — "clinician's case-note, in ink" (ADR-0026):** warm **paper & ink** neutrals (the diagrams' own monochrome ink-on-paper feel) with the **Okabe–Ito accents kept** (colourblind-safe, shared with `@psyuml/render`); a three-role type system — a restrained old-style **serif** (`--font-display`, system stack, zero web-font dep) for the wordmark/eyebrow/section headings, the humane **sans** for body, **mono** for ids/DSL; the **◎ "Self" glyph as the wordmark** + the diagrams' **dashed "held-lightly" stroke** on the honesty `note` + a **formulation-sheet** canvas (dot grid) as the signature; dark mode re-grounded as a *lamp-lit notebook* (warm darks, diagram kept on a light sheet). CSS custom properties — spacing/type scales, radii, shadows, focus ring — plus a **mobile-first + responsive** layout (no horizontal overflow at ~360px; richer multi-column at `min-width: 768/1024px`; ≥44px touch targets via `@media (pointer: coarse)`), `prefers-color-scheme: dark` (calm, AA), and `prefers-reduced-motion`. WCAG 2.2 AA contrast; colour stays redundant. **Edge hover/focus highlight (ADR-0025):** a `:has()` rule — hovering/keyboard-focusing an `[data-el^="edge:"]` keeps it bright + thickens it while the OTHER edges fade, so one relationship line is traceable through a crossing (nodes/labels untouched). **Print / paper copy (ADR-0038, REQ-EXPORT-RASTER):** an `@media print` block hides the interactive editor chrome (toolbar, the node/link editors, version/diff panels, buttons/inputs) and prints the **diagram + its honest "about" caption** on a white ground — the diagram SVG already draws the disclaimer, so it travels onto the page; browser *Save as PDF* is the PDF path | `main.tsx` (import) | build | REQ-ACCESSIBILITY, REQ-UX-STORIES, REQ-EDGE-CROSSING, REQ-EXPORT-RASTER | low |
| `App.tsx` | Editor: live render (all 20 diagram types **via the single `@psyuml/render` `render()` dispatcher**, monochrome passed through, + **zoom/fit/pan view controls** + **drag-to-reposition** on hand-laid-out diagrams (genogram/relational field, via the renderer's `data-node-id` hook → `setNodePosition`)), surfaced **Title** field + intro **Practitioner-handbook link** + an About-panel **"40+ diagram catalogue" link** (both base-aware `${import.meta.env.BASE_URL}…html`, open in a new tab — the docs are rendered to HTML by `scripts/build-docs.mjs` and published via `public/*.html`; guarded by `links.test.ts`), the **diagram picker grouped by v0.2 family** (`listFamilies`/`EXAMPLE_CATALOG`) — which doubles as the **interactive help site / example library (ADR-0020):** **74** real `examples/*.psyuml` grouped by family (54 per-school + disorder-specific + relational instances [incl. the REQ-CATALOG-COVERAGE batch — safety-behaviour, PTSD, eating-disorder, ecomap, cultural-genogram, …] + a **20-model ★ showcase set, one feature-dense model per diagram type** exercising the full notation, golden-pinned). **Example sources load via a Vite glob** (`import.meta.glob('../../examples/*.psyuml', { query:'?raw', eager })` → `EXAMPLES` keyed by slug), so a new example file is picked up automatically (no per-file import). **The non-showcase gallery items are DERIVED from `examples/catalog.json` (ADR-0028 single source)** — `import catalogManifest` → `EXAMPLE_CATALOG = [...manifest.diagrams.map(→ key/label/family/school/note), ...SHOWCASE_ITEMS]`; the ★ showcase set is the only hand-authored list. So a gallery item is its manifest row (conformance enforces the schema); the catalog prose, the gallery, and the corpus can't drift. Each item carries its originating school + a one-line **honest** `note` (blurb + evidence/limit), surfaced as an **"About this example"** card beside the live render and an **"About these diagrams — please read"** disclosure (the unvalidated-v0.x / honesty clause). Picking an item loads it into this same editor → live editing for free. A **v0.2 audience-profile selector** (clinician/client/picture; ADR-0016 — drives both the label layer and the interpretive-surface visibility; derives the `layer` used by validate/diff) + monochrome + **school** + **Language** selectors (ADR-0042/REQ-I18N-LOCALIZATION — lists the langs the current model carries via `availableLanguages`, flows `lang` to render so `getText` localizes; an RTL locale (`isRtl`) sets `dir="rtl"` on the diagram; the `i18n-demo` example is en·es·ar) + **live §K profile loading** (ADR-0040, REQ-LIVE-PROFILES — a Profile file input + `CFT`/`cultural-pack` quick-loads; `validateProfile` runs the §K rules incl. the §6 cultural-permission gate and `applyProfile` applies `roleLabelsFromProfile` **only if valid**, the same `roleLabels` surface as the built-in schools (which the selector disables while a profile is active); a profile panel surfaces the applied profile, every validation issue, and each cultural stereotype's tradition/RESTRICTED/permission/attribution; an invalid/un-permitted profile is shown but never applied; session-only, not persisted into the model), a gentle **symbol-budget nudge** (`withinSymbolBudget`, §2), **acute-risk/psychosis flags**, **New (blank)** + **Open .psyuml** + Save/Export (filenames from the title) with **the block reason shown on disabled buttons** — **Save .psyuml**, **Export SVG**, **Export PNG** (`downloadPng` rasterizes the deterministic SVG in-browser via `<canvas>` at 2× on a white ground, dependency-free; ADR-0038/REQ-EXPORT-RASTER) and **Print** (`window.print()` → the `@media print` stylesheet; Save-as-PDF for paper), **a picture-mode candidate-pictograph panel** (ADR-0037 — when the audience is `picture`, shows `pictographKeySvg()`'s "NOT validated" candidate sheet with copy that the diagram still uses words until a symbol passes the comprehension gate), **add-node form (kind + plain-language client label + stereotype datalist + provenance)**, node panel (rename / certainty / hide / remove + a **"more" disclosure: client label, stereotype, provenance**), **Links panel** (friendly connector names + a **⚑ trigger** option), **Diagram-details editor** (disclaimer/crisis), text alt, formulation-health panel + export gating, **clinical-escalation banner**, **Compare-with (diff)** + **version snapshots**, **Text (DSL) view/edit**, a **v0.2 Composite board with case-file authoring** (ADR-0039, REQ-CASE-FILE) — add the current view to the board, toggle `renderComposite` (several views of one case cross-linked by shared node id), **remove a specific view** (a per-view list), and **Save / Open a persisted `.psyuml-case` file** (`caseFileFrom`/`serializeCaseFile`/`parseCaseFile`) with a case-title field; the board **is** the case file's `documents` (a pure container of models). **Presentation now via semantic `className`s against `styles.css` (ADR-0013) — inline styles removed bar a few dynamic ones (zoom-driven canvas width); all aria-labels / roles / region + button names / `<summary>` texts preserved byte-identical. Editor opens in colour by default (`monochrome` initial `false`); Monochrome toggle kept as the print path.** **Identity + hierarchy (ADR-0026):** the masthead is a **◎-glyph + serif wordmark** lockup (the `<h1>` accessible name stays exactly `PsyUML editor`; the ◎ is `aria-hidden`); the **live diagram is promoted up under the controls** as the hero (located by `aria-label`, so reading/focus order + all e2e journeys are unchanged). A **post-render effect** decorates the injected SVG's edges with `tabindex`/`role`/`aria-label` (from `editor.edgeAriaLabel`) so the `styles.css` edge hover-highlight also works from the keyboard (`:focus-visible`) with a screen-reader announcement — trace one line through a crossing (ADR-0025). | `@psyuml/model`, `@psyuml/render`, `@psyuml/validate`, `@psyuml/profiles`, `@psyuml/diff`, `@psyuml/grammar`, `editor.ts`, `styles.css`, `examples/*.psyuml` | — | REQ-EDITOR-MVP, REQ-UX-STORIES, REQ-ACCESSIBILITY, REQ-COLLAB, REQ-CLIENT-SAFETY-UX, REQ-EPISTEMIC-STATUS, REQ-ETHICS-GUARDRAILS, REQ-NOTATION, REQ-PATH-OF-HOPE, REQ-SAFETY-TRIAGE, REQ-CROSS-SCHOOL, REQ-VERSIONING-DIFF, REQ-TEXT-DSL, REQ-AUDIENCE-PROFILES, REQ-DIAGRAM-FAMILIES, REQ-COMPOSITE-BOARD, REQ-EXAMPLE-LIBRARY, REQ-EDGE-CROSSING, REQ-PICTURE-PICTOGRAPHS, REQ-EXPORT-RASTER, REQ-CASE-FILE, REQ-LIVE-PROFILES, REQ-CULTURAL-PERMISSION, REQ-I18N-LOCALIZATION | medium |
| `editor.ts` | Pure editor-state helpers (`addNode` (kind/stereotype/**client label**/**provenance**/tier), `nextId`/`nextEdgeId`, `addEdge`/`removeEdge` (+**trigger**), `setNodeLabel`, `setNodeEpistemic`, `setNodeHidden`, **`setNodeStereotype`**, **`setNodeProvenance`**, **`setNodePosition`** (drag), `removeNode`, `setMeta`, `setSafetyFlag`, `snapshotModel`/`restoreVersion`, **`edgeAriaLabel`** (a plain-language "Link: <source> <relationship> <target>" for the edge hover/focus highlight's screen-reader announcement, ADR-0025)) — testable in node | `@psyuml/model` | `App.tsx`, `editor.test.ts` | REQ-EDITOR-MVP, REQ-COLLAB, REQ-CLIENT-SAFETY-UX, REQ-EPISTEMIC-STATUS, REQ-ETHICS-GUARDRAILS, REQ-NOTATION, REQ-CROSS-SCHOOL, REQ-SAFETY-TRIAGE, REQ-VERSIONING-DIFF, REQ-EDGE-CROSSING | low |
| `editor.test.ts` | Unit tests for editor helpers | `editor.ts` | CI `test` | — | low |
| `links.test.ts` | Guards the **deployed-link** bug class: runs `scripts/build-docs.mjs` and asserts the linked docs are published as **rendered HTML pages** (`handbook.html` / `diagram-catalog.html`, `<!doctype html>` + real `<table>`s — never raw `.md` served as plain text), that every `${import.meta.env.BASE_URL}…` href in `App.tsx` targets a file shipped in `public/`, and that no in-app link points at an undeployed repo path (`../docs/…`, `/docs/…`) or a raw `.md` | `App.tsx`, `index.html`, `public/`, `../../scripts/build-docs.mjs` | CI `test` | REQ-HANDBOOK, REQ-EXAMPLE-LIBRARY, REQ-EDITOR-MVP | low |
| `vite-env.d.ts` | Vite client ambient types (enables `?raw` imports) | `vite/client` | typecheck | — | low |

## Change checklist
- [ ] Keep the client layer plain/non-pathologizing; gate client exports on disclaimer + crisis fields (§L.2).
- [ ] When wiring `@psyuml/*`, update Upstream above and the dependency DAG in `sdd/README.md`.
- [ ] Ran `node sdd/check.mjs` (green).
