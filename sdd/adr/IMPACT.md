# Impact — `sdd/adr/`

**Purpose:** Architecture Decision Records — the durable log of *why* significant
choices were made.
**Status:** active
**Spec anchor / REQ:** cross-cutting

## Upstream (this depends on)
- `../templates/adr.md` — the format ADRs follow.

## Downstream (depends on this) — blast radius
> **Blast radius: low.** ADRs are a historical record; superseding one is additive (write
> a new ADR that supersedes it). They inform, but do not execute.
- Contributors and future ADRs (which may supersede earlier ones).

## Files
| File | Purpose | Upstream | Downstream | Spec / REQ | Change risk |
|---|---|---|---|---|---|
| `0001-adopt-spec-driven-development.md` | Records adopting the SDD harness + enforced impact docs | `../templates/adr.md` | the whole `sdd/` harness rationale | cross-cutting | low (append-only) |
| `0002-incorporate-psyml-source3.md` | Records adopting Source-3 (PsyML) ideas; keeps PsyUML notation as source of truth | `../templates/adr.md`, `../../docs/research/psyml-fable-agent-spec.md` | REQ-EPISTEMIC-STATUS, REQ-SAFETY-TRIAGE, REQ-BODY-MAP, REQ-I18N, REQ-PRIVACY, REQ-INTEROP-FHIR, REQ-EVAL-SUITE | §B, §J, §K, §L.2 | low (append-only) |
| `0003-adopt-ux-research-as-binding.md` | Records treating the UX research findings as binding requirements for the editor | `../templates/adr.md`, `../../docs/ux/ux-research-and-requirements.md` | REQ-UX-STORIES, REQ-COLLAB, REQ-CLIENT-SAFETY-UX, REQ-EDITOR-MVP, REQ-ACCESSIBILITY, REQ-DECISION-NAV | §D, §E.8, §L.2 | low (append-only) |
| `0004-bulk-coverage-for-generated-files.md` | Records the `sdd:cover` glob directive (generated/fixture files documented in bulk) | `../templates/adr.md`, `../check.mjs` | `sdd/check.mjs`, `examples/IMPACT.md` | cross-cutting | low (append-only) |
| `0005-client-facing-diagram-classification.md` | Records which diagram types are client-facing (disclaimer-gated) — adds `process-loop`, excludes `two-triangles`; + in-UI diagram-details editing | `../templates/adr.md`, `../../packages/validate/index.ts` | REQ-ETHICS-GUARDRAILS, REQ-PATH-OF-HOPE | §A.2-r7, §L.2 | low (append-only) |
| `0006-rendering-robustness-fit.md` | Records content-fit viewBoxes + `fitText` fit-to-box labels (no clipping / overflow on screen or export) found by the build-a-diagram agent test | `../templates/adr.md`, `../../packages/render/index.ts` | REQ-ACCESSIBILITY, REQ-NOTATION | §D | low (append-only) |
| `0007-cross-school-provenance-contested-origins.md` | Records the shared `schoolClaims` reader (bare + `school:` tags), validate firing on real data, and the Parts Map contested-origin marker (`⚖ A vs B`) + alt-text — making §G.2's "preserve disagreement" real | `../templates/adr.md`, `../../packages/model/index.ts` | REQ-CROSS-SCHOOL, REQ-ACCESSIBILITY | §G.2 | low (append-only) |
| `0008-region-neutral-crisis-resources.md` | Records keeping crisis defaults region-neutral (no baked-in per-country numbers) + the `safety.crisis-localize` nudge for a concrete local contact under acute risk | `../templates/adr.md`, `../../packages/validate/index.ts` | REQ-SAFETY-TRIAGE, REQ-DECISION-NAV | §A.2-r7, §L.2 | low (append-only) |
| `0009-extension-mechanism-validated-profiles.md` | Records implementing §K as a typed, validated profile registry in `@psyuml/profiles` (`ExtensionProfile`/`validateProfile`, the four rules + Tier-1 freeze + glyph-collision), the `lint-profile` CLI, and conformance invariants | `../templates/adr.md`, `../../packages/profiles/index.ts` | REQ-EXTENSION-MECH | §K, §B, §C | low (append-only) |
| `0010-scalable-cycle-aware-layout.md` | Records the cycle-aware longest-path layering for the Decision/Navigation chart (DFS back-edge break so a looping crisis plan doesn't collapse onto one row) + content-fit viewBox that grows to the widest layer + parallel-edge fan-out in the State Map + rendering the disclaimer into the decision-chart SVG | `../templates/adr.md`, `../../packages/render/index.ts` | REQ-DECISION-NAV, REQ-NOTATION, REQ-ACCESSIBILITY | §E.1, §E.8, §D | low (append-only) |
| `0011-eval-round-2-label-legibility-and-guards.md` | Records the second eval round's fixes — wrapping decision-chart node labels, haloing state-map parallel-edge labels, rendering the Parts Map containment label, generalizing "never silently drop" to edges (`render.edge-not-shown`), and the editor unsaved-changes confirm + Open selector sync | `../templates/adr.md`, `../../packages/render/index.ts`, `../../apps/web/App.tsx` | REQ-ACCESSIBILITY, REQ-NOTATION, REQ-DECISION-NAV, REQ-EDITOR-MVP | §D, §E | low (append-only) |
| `0012-guaranteed-non-overlap-layout.md` | Records the systematic non-overlap layout: a shared geometry layer (`layout.ts` — one `textWidth`, AABB `overlaps`/`union`, and the `separate1D` VPSC / Fast-Node-Overlap-Removal core), structure-aware placement + the scaling guarantee (grow canvas / content-fit so `separate1D` spreading never clips), `data-el` element tagging, and the machine-checked `overlap.test.ts` invariant over the corpus + 3 stress models. Scopes the guarantee under the shared text-metric (node↔node / label↔non-owner-node / in-frame universal; label↔label enforced for ten of twelve renderers, a documented known-gap for parts-map + process-loop whose free curve/chord labels collide; edge line/path crossings out of scope) | `../templates/adr.md`, `../../packages/render/index.ts`, `../../packages/render/layout.ts` | REQ-ACCESSIBILITY, REQ-NOTATION, REQ-CONFORMANCE | §D, §J | low (append-only) |
| `0013-editor-design-system-responsive.md` | Records the editor's hand-rolled, token-based design system (new `../../apps/web/styles.css` — CSS custom properties for an Okabe–Ito-grounded palette, spacing/type scales, radii, shadows, focus ring), the mobile-first + responsive layout (no horizontal overflow at ~360px; richer multi-column at `min-width` breakpoints; ≥44px touch targets via `pointer: coarse`), colour-by-default in the editor (initial `monochrome` → `false`, toggle kept, renderer's monochrome-by-default library default untouched), the no-UI-framework / no-new-dependency stance, and the WCAG 2.2 AA + colour-redundant posture; `App.tsx` moved from inline styles to semantic `className`s with all a11y/semantic hooks preserved byte-identical | `../templates/adr.md`, `../../apps/web/styles.css`, `../../apps/web/App.tsx` | REQ-UX-STORIES, REQ-ACCESSIBILITY, REQ-EDITOR-MVP | §D | low (append-only) |

| `0014-v0.2-collaborative-formulation-evolution.md` | Records the **v0.2** evolution (backward-compatible): collaborative-formulation centre + honesty clause, 8 diagram families × 3 audience profiles, CAT Pattern semantics (ontology-neutral), comprehension-testing gate (ISO 9186), lossy FHIR export, sharpened ethics; M11 ships the families/profiles registry | `../templates/adr.md`, `../../docs/specification/psyuml-v0.2.0.md`, `../../docs/research/v0.2-research-synthesis.md`, `../../packages/profiles/index.ts` | REQ-DIAGRAM-FAMILIES, REQ-AUDIENCE-PROFILES, REQ-NOTATION-TESTING, REQ-INTEROP-FHIR | v0.2 §0–§10 | low (append-only) |
| `0015-v0.2-pattern-semantics-provenance-rendering.md` | Records the concrete **M12** realization choices: extend `EpistemicStatus` (add `jointly-agreed`/`clinician-inferred`/`contested`) rather than a parallel field; `Edge.loopTopology` (trap/dilemma/snag) as an edge field; `Properties.asIf` ontology-neutral flag; render conventions (centre topology glyph + redundant word, dashed=interpretive, ⚖/as-if on the owner label, alt-text echoes all); DSL parity. All additive — only `cat-sdr.svg` changed | `../templates/adr.md`, `0014-v0.2-collaborative-formulation-evolution.md`, `../../packages/model/index.ts`, `../../packages/render/index.ts`, `../../packages/grammar/index.ts` | REQ-PATTERN-SEMANTICS, REQ-PROVENANCE-CONFIDENCE | v0.2 §3, §4 | low (append-only) |
| `0016-v0.2-audience-profile-rendering.md` | Records the **M13** audience-profile rendering: a single `render()` dispatcher hosting audience→layer/`showInterpretive` resolution (explicit wins → no golden churn); gate the clinician-analytic surface on `showInterpretive` in `renderLoopMap` + `renderPartsMap` (dashed border kept in every profile); editor family-grouped picker + audience selector + `withinSymbolBudget` nudge | `../templates/adr.md`, `0014-v0.2-collaborative-formulation-evolution.md`, `../../packages/render/index.ts`, `../../packages/profiles/index.ts`, `../../apps/web/App.tsx` | REQ-AUDIENCE-PROFILES, REQ-EDITOR-MVP, REQ-ACCESSIBILITY | v0.2 §2, §3 | low (append-only) |
| `0017-v0.2-notation-comprehension-testing.md` | Records the **M14** comprehension-testing realization: ship the buildable half honestly — a `NOTATION_SYMBOLS` registry + a Tier-A `auditNotation()` harness (discriminability + dual-coding of safety symbols + gloss, CI-enforced) + the ISO 9186 Instrument D & per-symbol ledger (every Tier-B cell `pending`); the Tier-B human studies stay the v0.x gate and results are **never fabricated** | `../templates/adr.md`, `0014-v0.2-collaborative-formulation-evolution.md`, `../../packages/profiles/index.ts`, `../../docs/adoption/comprehension-instruments.md`, `../../docs/evaluation-suite.md` | REQ-NOTATION-TESTING, REQ-EVAL-SUITE | v0.2 §5 | low (append-only) |
| `0018-v0.2-lossy-fhir-export.md` | Records the **M15** lossy, export-only FHIR bridge: new `@psyuml/interop` (isolated leaf) `toFhir` → a document Bundle per the §7 table (Observation never Condition; CarePlan/Goal; FamilyMemberHistory/List) + a structured `loss` report + `validateFhirBundle` (subset + reference integrity, not a full validator); de-identified + audience-scoped by default; **no importer / round-trip**; surfaced as `psyuml export` | `../templates/adr.md`, `0014-v0.2-collaborative-formulation-evolution.md`, `../../packages/interop/index.ts`, `../../packages/privacy/index.ts`, `../../packages/cli/index.ts` | REQ-INTEROP-FHIR, REQ-PRIVACY | v0.2 §7, §6 | low (append-only) |
| `0019-v0.2-composite-board-cultural-packs.md` | Records the **M16** close-out: `renderComposite(models)` + `sharedNodeIds()` give the Composite family real output (member views nested over a shared-id cross-navigation index; an array of models, no schema change); and a `StereotypeDef.cultural` block + the `profile.cultural-permission` `validateProfile` rule (a closed/restricted symbol MUST declare permission) + the worked `CULTURAL_PACK_EXAMPLE`. Both additive; accepted nit: nested member SVGs duplicate the `arrow` marker def | `../templates/adr.md`, `0014-v0.2-collaborative-formulation-evolution.md`, `../../packages/render/index.ts`, `../../packages/profiles/index.ts` | REQ-COMPOSITE-BOARD, REQ-CULTURAL-PERMISSION, REQ-DIAGRAM-FAMILIES, REQ-EXTENSION-MECH | v0.2 §2, §6, §K | low (append-only) |
| `0056-mobile-touch-ux.md` | Records the **mobile / touch-screen UX hardening** (REQ-MOBILE-TOUCH-UX → implemented) — a phone audit (360×760, touch, pointer:coarse) found ADR-0013's mobile foundation had holes: 286px horizontal overflow (Composite-board `.row` lacked flex-wrap; a native file input poked ~25px past the edge), 12–14px form fonts (iOS focus-zoom), a 43.4px summary tap target, and no tap-highlight/overscroll/touch-action. Fixed: `.row` wraps; the file-action group is a full-width touch row + `:has(>input[type=file])`; `pointer:coarse` lifts fields to 16px and a new `--fs-mono` (12→16px) feeds the DSL textarea; all summaries 44px; `-webkit-tap-highlight-color:transparent`, `overscroll-behavior:contain`, `touch-action:manipulation`. (Touch-drag + a zoom control were already sound.) Enforced two ways: `e2e/mobile.spec.ts` MEASURES it in a browser (no overflow 320/360px, fields ≥16px, controls ≥44px, drag wired) and `apps/web/mobile-ux.test.ts` is the CI-enforced rule guard (incl. WCAG 1.4.4/1.4.10 'never disable zoom'). 5 e2e mobile + 12 desktop e2e + 7 CI tests green; TDD-proven to bite | `../templates/adr.md`, `0013-editor-design-system-responsive.md`, `0026-editor-visual-identity-case-note.md`, `../../apps/web/styles.css`, `../../e2e/mobile.spec.ts` | REQ-MOBILE-TOUCH-UX | §D | low (append-only) |
| `0055-visual-quality-study.md` | Records the **simulated visual-quality study** (REQ-VISUAL-QUALITY-STUDY → implemented) — the geometry invariants run over the hand-tuned `examples/` corpus, so defects that only appear under DENSE custom input (this session: off-the-body markers, title-shoved rings, 40%-squished labels) had nowhere to surface. New `study-cases.ts` = 8 ADVERSARIAL diagrams (process-loop/parts-map/body-map/state-map/decision-nav/relational-field/mode-map/intervention-sequence, each maxing out its type — many nodes, long labels, custom positions, every optional channel) authored as validated `parseModel` objects, + `checkVisualQuality(svg,label,diagram)`, ONE battery aggregating the corpus invariants (node/label/text non-overlap, in-frame, interior containment [LABEL_IN_BOX-scoped], no-severe-squish, microtext floor, gross empty-margin, loop-map ring-centring). `study-cases.test.ts` renders each case × both layers, asserts zero violations; TDD-proven to bite (planted loop-map frame-growth → 'ring off-centre by −62px'). Cases NOT golden-pinned (assert quality, not bytes). HONEST SCOPE: simulates the DRAWING + legibility half only — comprehension/utility/safety need the human studies (REQ-STUDY-PREREG); balance checks conservative (loop-scoped/gross) to avoid false positives on structured layouts | `../templates/adr.md`, `0012-guaranteed-non-overlap-layout.md`, `0045-text-legibility-invariant.md`, `0054-titles-wrap-not-grow.md`, `../../packages/render/study-cases.ts` | REQ-VISUAL-QUALITY-STUDY | §D, §J | low (append-only) |
| `0054-titles-wrap-not-grow.md` | Records the **titles-wrap-not-grow** balance fix (REQ-CONTENT-BALANCE → implemented) — ADR-0052's title-fit follow-up made content-fit renderers GROW the frame to fit a long title, but the body stayed put while the frame grew right, leaving it off-centre with an empty right margin (worst on loop-maps: a ~390px ring + long titles → ring parked in the left ~60%, eating-disorder 61px off; the body-map had the same bug). No geometry guard caught it ('fits the box' ≠ 'looks centred'). Fix: `renderLoopMap`'s title WRAPS to ≤2 lines within the content width (`fw = ringWidth + 2·pad`, `wrapLines` + per-line `fitText`) and never grows the frame, so the ring stays centred. GUARD: `layout-quality.test.ts` invariant D asserts the loop-map ring node bbox is horizontally centred (|off| ≤ 24px — passes ~18px CAT-SDR structural asymmetry, catches the 32–61px growth shift; TDD red on a planted frame-widen). 8 process-loop goldens regenerated. HONEST SCOPE: a general content-balance invariant is too noisy (structured layouts are asymmetric by design), so the guard is loop-map-scoped; other content-fit renderers still grow (ADR-0052), latent-only while their titles fit | `../templates/adr.md`, `0052-text-in-frame-invariant.md`, `../../packages/render/index.ts`, `../../packages/render/layout-quality.test.ts` | REQ-CONTENT-BALANCE | §D, §J | low (append-only) |
| `0053-shrink-before-squish.md` | Records the **shrink-before-squish** legibility fix (REQ-LEGIBLE-COMPRESSION → implemented) — `fitText`/`wrapLabel` kept a long label in its box via horizontal `textLength` compression with NO floor, so a corpus sweep found labels crushed to ~40% of natural width (illegible) yet passing every invariant (they fit their box). Fix: a new `legibleFitSize(s,size,maxWidth)` prefers a smaller FULL-PROPORTION font over squishing — fitting labels unchanged; MILD overflow (≥ SQUISH_FLOOR 0.8) keeps size + textLength (byte-identical); SEVERE overflow shrinks toward the 8px floor so glyphs keep their shape, compressing only if still over at the floor. `fitText` uses it, `wrapLabel` re-wraps after shrinking, the 3 direct-textLength sites (venn contents, resource-anchor headers, decisional-balance cells) call it inline. GUARD: a `legibility.test.ts` invariant asserts every textLength segment is ≥ SQUISH_FLOOR OR at the 8px floor (TDD, red at 40-72% when disabled). Worst-case ~40% → ≥80%; invariant-safe (smaller fits better); only 6 goldens changed (mild compressions byte-identical) | `../templates/adr.md`, `0006-rendering-robustness-fit.md`, `0045-text-legibility-invariant.md`, `../../packages/render/index.ts`, `../../packages/render/legibility.test.ts` | REQ-LEGIBLE-COMPRESSION | §D, §J | low (append-only) |
| `0052-text-in-frame-invariant.md` | Records the **text-in-frame invariant + chrome-fit** (REQ-TEXT-IN-FRAME → implemented) — the in-frame analogue of ADR-0045 (text↔text) and ADR-0012's box in-frame check. `overlap.test.ts` guards only `data-el` boxes, so UNTAGGED chrome text (title, caption, disclaimer, genogram legend) could spill past the viewBox and CLIP on export/embed unseen. Found on the body-map (fixed 460px frame clipped the title + both bottom lines, caption by ~220px), then a sweep showed it PERVASIVE — 38 layer-cases / ~10 renderers (every process-loop title; the genogram legend; many raw full-width captions/disclaimers). New `text-overlap.test.ts` invariant asserts every reconstructed `<text>` AABB (`textBoxesFromSvg`, honouring textLength) sits inside the SVG viewBox (every example × both layers, ~4px tol). Fix: a shared `chromeLine(s,x,y,frameW,opts)` compresses bottom chrome to frame width (byte-identical when it already fits → no churn) for the overflowing captions/disclaimers (mode-map, ladder, three-circles, venn, resource-anchor, schema-grid, decisional-balance, secure-base); two cases get ROOM not compression — process-loop titles (frame grows wider) + genogram legend/title (frame grows to fit the glyph strip) — and the body-map caption/disclaimer WRAP with the frame height growing. 16 goldens regenerated; all geometry invariants hold. HONEST SCOPE: helper applied where overflow exists today, the rest guarded by the invariant + converted lazily | `../templates/adr.md`, `0045-text-legibility-invariant.md`, `0012-guaranteed-non-overlap-layout.md`, `0050-resource-diamond-label-placement.md`, `../../packages/render/index.ts`, `../../packages/render/text-overlap.test.ts` | REQ-TEXT-IN-FRAME | §D, §J | low (append-only) |
| `0051-crisis-caption-clears-its-box.md` | Records **closing the last caption-bisection gap** (`CAPTION_BISECT_KNOWN_GAP = {decision-nav}` → empty; extends REQ-TEXT-CONTAINMENT) — the same dense-diagram audit (ADR-0050) re-examined ADR-0046's one documented exception on the **safety-critical** crisis node: the "! Call a crisis line now" box's crisis-resources caption had its TOP line riding up under the thick bottom border. Root cause was a CENTRING bug, not lack of room — `wrapLabel` centres its block, so a fixed `+13` offset placed the 3-line block's top line ~1px below the box bottom (ascenders inside the border); and under the ADR-0049 layered layout the depth-2 boxes sit in the centre/right lanes (not below the left crisis node), leaving ~20px unused. Fix: shift the caption block DOWN by half its height (`cy = boxBottom + 13 + (lines−1)·lh/2`) so the first line always clears the border whatever the line count (measured: box bottom y=185 → caption top y≈191). `decision-nav` removed from the known-gap → invariant C is now UNIVERSAL (every renderer keeps captions clear; +8 enforced cases). No layout change; 2 goldens regenerated (decision-nav, showcase-decision-nav); the sibling-collision ADR-0046 worried about is now actively checked (overlap + text-overlap + layout-quality C green), not asserted away | `../templates/adr.md`, `0046-text-containment-invariant.md`, `0049-layered-sugiyama-layout.md`, `0050-resource-diamond-label-placement.md`, `../../packages/render/index.ts`, `../../packages/render/layout-quality.test.ts` | REQ-TEXT-CONTAINMENT | §D, §J | low (append-only) |
| `0050-resource-diamond-label-placement.md` | Records the **loop-map resource-label containment** fix (extends REQ-TEXT-CONTAINMENT to the diamond shape) — a dense-diagram audit (ADR-0049 follow-up) found the process-loop **resource diamond**'s label drawn INSIDE the flat 140×44 rhombus poked the slanted edges (a rhombus has its full width only on its centre line; the diamond can't grow without breaking the ring spacing → overlap; 2 lines can't be inscribed in a 44px-tall rhombus). Fixed by placing the label by LENGTH: a SHORT label (`textWidth ≤ 100px`, one line) stays INSIDE the widest band (fills the glyph as a normal node — client "Ride it out"); a LONG label is lifted OUTSIDE on the outward (away-from-ring-centroid) vertical side — above when the diamond is upper, below otherwise — wrapped to ≤3 lines at full width, white-haloed (ADR-0047), with the frame grown past it. Outward is open exterior AND away from the inward exit-chord's own midpoint label, so the node label never collides with it. GUARD: a new `legibility.test.ts` invariant asserts every resource-label line is wholly inside the rhombus OR wholly clear of the diamond box (never edge-cutting), both layers — TDD, proven red on 18 layer-cases when forced inside. 8 process-loop goldens regenerated; all geometry invariants hold. HONEST SCOPE: a future ring hemmed on a diamond's outward side is caught by the overlap/text-overlap invariants, not silently shipped | `../templates/adr.md`, `0049-layered-sugiyama-layout.md`, `0046-text-containment-invariant.md`, `0047-label-legibility-halos.md`, `../../packages/render/index.ts`, `../../packages/render/legibility.test.ts` | REQ-TEXT-CONTAINMENT | §D, §J | low (append-only) |
| `0049-layered-sugiyama-layout.md` | Records the **proper layered (Sugiyama) layout** (REQ-LAYERED-LAYOUT → implemented) — decision-nav did only steps 1-2 (cycle-break + longest-path) then ordered within-layer by MODEL order and centred each layer alone, skipping the SOTA quality steps. New deterministic `packages/render/layered.ts` (`layeredLayout`) does the full pipeline: cycle removal + longest-path rank, **crossing minimisation** (median heuristic + adjacent-transpose), and **coordinate assignment** (median alignment with VPSC `separate1D` separation — straightens parent→child edges; a convergence node centres under its parents). `renderDecisionChart` uses it; `layerWithCycleBreak` deleted. Unit-tested + corpus geometry invariants hold (decision-nav stays crossing-free); 3 decision-nav goldens regenerated. HONEST SCOPE: median coords not full Brandes-Köpf (gold standard, noted as the upgrade — mirrors `separate1D` 1-D-core/full-VPSC); no dummy nodes for long edges (multi-layer crossings approximate); applies to the FREE-ordering layered diagram (decision-nav) — intervention-sequence is swimlane-constrained; force/stochastic layout rejected (unfit for goldens) | `../templates/adr.md`, `0010-scalable-cycle-aware-layout.md`, `0023-no-engine-adopt-a-router.md`, `../../docs/research/layout-algorithms.md`, `../../packages/render/layered.ts` | REQ-LAYERED-LAYOUT | §D, §J | low (append-only) |
| `0048-editor-deep-linking.md` | Records **editor deep-linking** (REQ-DEEP-LINK → implemented) — fixes the broken browse → edit flow: the showcase's only path into the editor landed on the default `state-map`, not the diagram being read. A pure node-tested helper `exampleKeyFromQuery(search, available, fallback)` (apps/web/editor.ts) opens `?example=<key>` IF it names a shipped example, else falls back safely (only exact known keys load — no off-corpus/remote fetch); App.tsx resolves it at mount + mirrors the current example into the URL via `replaceState` (shareable/bookmarkable/refresh-stable). Each showcase card gains an 'Open this diagram in the editor →' deep-link (`./?example=showcase-<type>`, base-aware), guarded by `conformance/showcase.test.ts`. Verified end-to-end (Playwright). HONEST SCOPE: shipped-corpus keys only; `replaceState` not `pushState`; one query param, not a router or model-in-URL (privacy) | `../templates/adr.md`, `0044-printable-showcase.md`, `../../apps/web/editor.ts`, `../../apps/web/App.tsx`, `../../scripts/build-showcase.mjs` | REQ-DEEP-LINK | §D | low (append-only) |
| `0047-label-legibility-halos.md` | Records the **legibility-halo pass** (REQ-LABEL-HALO → implemented) — a pervasive low-grade issue (a word drawn on/beside a line read with the line striking through it: process-loop 'trigger', mode-map 'attacks'/dom, two-triangles 'hides'/'stirs', intervention-seq guards, secure-base banners crossing the circle, body-map sensations crossing the outline) fixed with the established white under-glyph halo (`paint-order="stroke"`), previously only ad hoc (4 of 8 raw edge labels; `fitText`/`wrapLabel` had none). Made the halo a first-class `TextOpts.halo` option of the shared helpers (+ `haloAttr`) and applied it to every edge label + over-decoration node label. GUARD: `legibility.test.ts` asserts every `data-el="edgelabel:"` carries `paint-order="stroke"` (every example × both layers) → a future bare edge label fails CI. Purely additive (stroke under existing glyphs) — geometry unchanged, all overlap/containment/crossing invariants untouched; 25 goldens regenerated | `../templates/adr.md`, `0011-eval-round-2-label-legibility-and-guards.md`, `0045-text-legibility-invariant.md`, `../../packages/render/index.ts`, `../../packages/render/legibility.test.ts` | REQ-LABEL-HALO | §D, §J | low (append-only) |
| `0046-text-containment-invariant.md` | Records the **containment invariant** (REQ-TEXT-CONTAINMENT → implemented) — the companion to ADR-0045 for the OTHER showcase-audit class: text clipped by its OWN container shape. The clear defect (mode-map names overflowing their dominance-sized circles) is fixed by moving the name + `dom` numeral to a caption BELOW the circle → the circle becomes a PURE dominance glyph (nothing to clip; cleaner notation) and the name is now covered by the ADR-0045 text↔text check. New `layout-quality.test.ts` invariant C asserts every node label whose CENTRE is outside its node is DISJOINT from it (never straddling the border) — so with invariant A (centre-inside ⇒ contained) every owner label is cleanly IN or OUT, never bisected. HONEST SCOPE: interior glyph-label tightness (parts-map/relational-field labels filling their symbol nodes) is INTENDED notation within A's 3px slop, not in scope; `CAPTION_BISECT_KNOWN_GAP = {decision-nav}` is the one documented graze (its crisis caption sits tight under its thick-bordered node in the compact crisis chart, where pushing it clear collides with a branch label); text-over-decoration stays intentional. 2 mode-map goldens regenerated | `../templates/adr.md`, `0021-layout-quality-invariants.md`, `0045-text-legibility-invariant.md`, `../../packages/render/layout-quality.test.ts`, `../../packages/render/index.ts` | REQ-TEXT-CONTAINMENT | §D, §J | low (append-only) |
| `0045-text-legibility-invariant.md` | Records the **text-legibility invariant** (REQ-TEXT-LEGIBILITY → implemented) — a visual audit of the 20 showcases found a collision class the machine invariants were blind to: `overlap.test.ts` (ADR-0012) guards only `data-el` node/label boxes, so UNTAGGED chrome text (title, band/lane headers, per-region contents, captions, provenance footnotes, legends) could collide unseen (14 pairs across 12 corpus files, 5 classes). New `introspect.textBoxesFromSvg` reconstructs the AABB of EVERY `<text>` (halos are a white stroke on the same element → counted once); new `packages/render/text-overlap.test.ts` asserts no two text boxes overlap for every example × both layers. Five renderer fixes made it green: `renderInterventionSeq` reserves a title band (title can't overlap lane headers); `renderLoopMap` reserves the centre R/B + loop-topology badge as a `deCollide` obstacle so chord labels avoid it; `renderVenn` caps each region's contents to its zone; `renderPartsMap` widens the footnote↔legend gap; `renderResourceMap` caps each column header. 11 goldens regenerated; full render+conformance suite green. HONEST SCOPE: text↔text only — text over DECORATION (banner on a circle, label on a body outline, edge label on its own connector) is intentional labelling, NOT a collision; text staying inside its own container shape is the separate containment guarantee (ADR-0046). TDD: written first, red on 24 layer-cases, green after the fixes | `../templates/adr.md`, `0012-guaranteed-non-overlap-layout.md`, `0024-edge-router-interface-two-backends.md`, `../../packages/render/text-overlap.test.ts`, `../../packages/render/introspect.ts` | REQ-TEXT-LEGIBILITY | §D, §J | low (append-only) |
| `0044-printable-showcase.md` | Records the **standalone, drift-proof HTML showcase** (REQ-SHOWCASE-PAGE → implemented) — one worked example of every diagram type, with explanations, an **on-paper** drawing guide, and honesty notes, framed as four jobs-to-be-done (read / draw-on-paper / choose / use-honestly). Single-source `examples/showcase.json` (editorial prose only — titles come from the models, renders from the committed `showcase-<type>.svg` goldens, `family`/chooser from `FAMILY_OF`/`FAMILIES`); a bespoke `scripts/build-showcase.mjs` (exports `renderShowcase`/`esc`) emits a self-contained `apps/web/public/showcase.html` (inline CSS + inline SVG, no external assets, light/dark, sticky family nav, print stylesheet so it prints onto paper). Drift-locked by `conformance/showcase.test.ts` (TDD: manifest vs system — `diagrams` == `DiagramType.options`, `family` == `FAMILY_OF`, chooser == `FAMILIES`; generated page vs manifest) and link-checked by `apps/web/links.test.ts`; linked from the editor. HONEST SCOPE: one example per type (not the 40+ catalogue); editorial prose hand-authored (guard locks structure, not wording); `pattern`/`composite` render empty + are noted honestly; output gitignored like the other published pages | `../templates/adr.md`, `0027-catalog-corpus-conformance.md`, `0043-docs-conformance.md`, `../../scripts/build-showcase.mjs`, `../../examples/showcase.json` | REQ-SHOWCASE-PAGE, REQ-EXAMPLE-LIBRARY | §E | low (append-only) |
| `0043-docs-conformance.md` | Records making the **prose docs un-driftable** (REQ-DOC-CONFORMANCE → implemented) — the prose analogue of the catalog↔corpus guard (ADR-0027/0028). After the eight ◇ renderers landed, several docs still said "12 diagram types" and the format-reference authoring quick-guide listed only the original 12; a human had to reconcile each by hand. New `conformance/docs-conformance.test.ts` derives the load-bearing facts from the **system** — `DiagramType.options` (count + exact type list), `FAMILIES.length`, the `examples/` corpus (one `showcase-<type>` per type) — and asserts the docs match: the quick-guide table lists EXACTLY `DiagramType.options`; every digit-form "N diagram types/renderers" equals the renderer count; every "N families" equals the family count; one showcase per type. So adding the 21st type forces the row + counts or CI fails — the drift can't return. Also reconciled the residual drift (quick-guide completed to 20 rows; v0.2 spec / roadmap-to-v1 / ROADMAP / ARCHITECTURE / render+profiles IMPACTs counts + statuses brought current). HONEST SCOPE: digit-form counts only ("eight families" is unchecked prose); a `(?<![.\w])` lookbehind excludes version/identifier digits ("v0.1 diagram types", "M15"); `docs/research/**` (own drift check) + `sdd/adr/**` (append-only) out of scope. TDD: written first, red on real drift, green after fix, proven with a planted 20→19 regression | `../templates/adr.md`, `0027-catalog-corpus-conformance.md`, `0028-catalog-manifest-single-source.md`, `../../conformance/docs-conformance.test.ts`, `../../docs/format-reference.md` | REQ-DOC-CONFORMANCE | §J, §E | low (append-only) |
| `0042-i18n-localization.md` | Records **usable localization** (REQ-I18N-LOCALIZATION → implemented) — the i18n *architecture* existed (REQ-I18N: concept-keyed labels + `getText(…,lang)`), this makes it usable. `@psyuml/model` gains `isRtl(lang)` (RTL base subtags) + `availableLanguages(model)` (the langs a model's labels actually carry, `model.language` first). The editor gains a **Language** selector listing those langs (disabled when monolingual); `activeLang` flows to `render`/`renderComposite` and clamps to a language the model has; an **RTL** locale sets `dir="rtl"` on the diagram container (browser bidi shaping). One **illustrative** demo pack `examples/i18n-demo.psyuml` (a generic process-loop in en · es · ar) exercises it; its disclaimer says the translations are basic + NOT professionally validated. HONEST SCOPE (recorded): renderer **chrome** stays English (the safety "EXIT", ladder "harder/easier", banners, legends — only model *content* localizes); RTL is text-direction, not geometry mirroring; one demo pack, not fabricated clinical packs; `meta.title`/`disclaimer` are monolingual. Model unit tests + a Playwright e2e (en→es→ar with the `dir` flip) | `../templates/adr.md`, `../../packages/model/index.ts`, `../../apps/web/App.tsx`, `../../examples/i18n-demo.psyuml` | REQ-I18N-LOCALIZATION, REQ-I18N | §3 | low (append-only) |
| `0041-provenance-narrative.md` | Records the **contested-origin disagreement narrative** (REQ-PROVENANCE-NARRATIVE → implemented) — the "how" behind ADR-0007's ⚖ "which". A new optional `Properties.provenanceNote` (additive) captures HOW the schools in `provenance` differ on an element; `renderPartsMap` draws it as a **footnote** above the legend (`⚖ <name>: <note>`, plain text — not `data-el`, overlap-untouched; canvas content-fits) **and** appends it to **alt-text** (`How they differ — …`), both gated to the clinician/interpretive surface (client/picture hide it); the ⚖ marker (which) is unchanged and nothing is merged (§G.2). `validate` adds an `info` nudge `provenance.narrative-missing` when a contested node lacks a note (never blocks). Free text (one narrative), not a structured per-school table — disagreements aren't always a clean split. The showcase parts-map's "inner critic" gains a real note (+golden); model/render/validate tests. HONEST: parts-map only for the visible footnote (the loop-map gets the field + alt-text path, footnote is a follow-up) | `../templates/adr.md`, `0007-cross-school-provenance-contested-origins.md`, `../../packages/model/index.ts`, `../../packages/render/index.ts`, `../../packages/validate/index.ts` | REQ-PROVENANCE-NARRATIVE, REQ-CROSS-SCHOOL | §G.2, §3 | low (append-only) |
| `0040-live-extension-profiles.md` | Records **live §K profile / cultural-pack loading in the editor** (REQ-LIVE-PROFILES → implemented), reusing the existing machinery with **no new validation**. A **Profile (§K)** file input + two **Try** quick-loads (`CFT_PROFILE`/`CULTURAL_PACK_EXAMPLE`); `applyProfile` runs `validateProfile` and applies the profile **only if `ok`** — an invalid / **un-permitted** profile (a `restricted` symbol with no `permission` errors the §6 `profile.cultural-permission` rule) is shown with its errors and **never applied**. The applied profile's `roleLabelsFromProfile` vocabulary is passed to `render` as `roleLabels` — **the exact surface the built-in `school` selector uses** (which is disabled while a profile is active). A profile panel surfaces the applied profile + every issue + (leading) each cultural stereotype's **tradition / RESTRICTED / permission / attribution**, so a closed symbol's authorization travels with it. HONEST SCOPE: "apply" = the cross-school **vocabulary** (labels), NOT per-stereotype custom glyphs (the renderer is glyph-by-type — future); profiles are session-only (not persisted into the model/case file, keeping the permission boundary clean). A Playwright e2e applies the cultural pack + asserts the §6 RESTRICTED permission shows | `../templates/adr.md`, `0009-extension-mechanism-validated-profiles.md`, `0019-v0.2-composite-board-cultural-packs.md`, `../../apps/web/App.tsx` | REQ-LIVE-PROFILES, REQ-EXTENSION-MECH, REQ-CULTURAL-PERMISSION | §K, §6, §3 | low (append-only) |
| `0039-case-file-composite-authoring.md` | Records a **persisted case file + editor composite authoring** (REQ-CASE-FILE → implemented), additive over ADR-0019's in-memory/render-only Composite board. A `CaseFile` schema in `@psyuml/model` — `{ version, kind: 'case-file', meta: { title?, note? }, documents: PsyumlModel[] }` — is a **pure container** (each document is a fully-validated `PsyumlModel`, so **never** a model-schema change); the `kind: 'case-file'` literal is the **discriminator** so `parseCaseFile` rejects a single model. `parseCaseFile`/`serializeCaseFile` round-trip losslessly; `caseFileFrom(documents, meta)` builds one. The editor's board **is** the documents: the Composite panel gains a case-title field, **Save case file** (`<slug>.psyuml-case`), **Open case file**, and a **per-view list with Remove** — real authoring, not just append/clear; `renderComposite(board)` is unchanged. HONEST: a case file **aggregates several views of one person → more identifying**; §L.2 + de-identification (REQ-PRIVACY) unchanged + per-document. A model round-trip/discrimination unit test + a Playwright e2e (author 2 views → save → clear → reopen → board repopulates) | `../templates/adr.md`, `0019-v0.2-composite-board-cultural-packs.md`, `../../packages/model/index.ts`, `../../apps/web/App.tsx` | REQ-CASE-FILE | §3, §2 | low (append-only) |
| `0038-raster-print-export.md` | Records **PNG export + a print/PDF stylesheet** (REQ-EXPORT-RASTER → implemented), dependency-free. `downloadPng` rasterizes the deterministic SVG **in the browser** — read the `viewBox`, draw into an `<Image>`, paint onto a `<canvas>` at 2× over an opaque white ground (no black-on-dark), `canvas.toBlob('image/png')` → download (the SVG has no external refs, so the canvas isn't tainted); an **Export PNG** button sits beside Export SVG (same health gate). A Print button + an `@media print` block (`styles.css`) hide the editor chrome and print the **diagram + its honest caption** (the SVG already draws the disclaimer, so it travels); browser **Save as PDF** is the PDF path — **no rasterizer / PDF library bundled** (ADR-0013 no-new-dep stance kept). The CLI still emits SVG only (canvas is browser-only — honest scope). A Playwright e2e captures the download + asserts a real PNG (signature + size) | `../templates/adr.md`, `0013-editor-design-system-responsive.md`, `../../apps/web/App.tsx`, `../../apps/web/styles.css` | REQ-EXPORT-RASTER | §3 | low (append-only) |
| `0037-picture-pictographs.md` | Records **picture-profile pictographs, honestly gated** (REQ-PICTURE-PICTOGRAPHS → implemented, the buildable half) — the ADR-0017 pattern applied to the picture audience. Builds the **mechanism + candidate registry + Tier-A gate** in `@psyuml/profiles`, wired **inert**: a `PICTOGRAPHS` set of drawn 24×24 candidate icons (self/part/feeling/resource/exit/trigger/reach-out), each dual-coded with a word, all `comprehension: 'pending'`; **`auditPictographs`** (discriminable icons + every symbol dual-coded + gloss/id + the **honesty gate**: while `PICTOGRAPH_STUDY_RUN` is false, every symbol MUST stay `pending`, so an unvalidated symbol can never be marked `passed`/shipped); **`pictographFor`** returns nothing until a symbol passes (so the picture profile stays "client + flag" — no diagram golden changes); **`pictographKeySvg`** renders the candidate sheet with a "CANDIDATE … NOT validated" banner; the editor shows it in picture mode. The **Tier-B comprehension study stays the v1.0 gate** (REQ-STUDY-PREREG) and is never fabricated; the ledger (`comprehension-instruments.md`) gains a pictograph section, all `pending` | `../templates/adr.md`, `0016-v0.2-audience-profile-rendering.md`, `0017-v0.2-notation-comprehension-testing.md`, `../../packages/profiles/index.ts`, `../../apps/web/App.tsx` | REQ-PICTURE-PICTOGRAPHS, REQ-STUDY-PREREG | §1, §8, §5 | low (append-only) |
| `0036-secure-base-diagram-type.md` | Records the **eighth and LAST ◇ renderer** — **closing REQ-NEW-DIAGRAM-TYPES → implemented**: a 20th `DiagramType` **`secure-base`** + `renderSecureBase`. Catalog #56 is "Circle of Security", a **registered trademark** + copyrighted graphic whose own catalogue note warns "graphic ≠ program" (a 2025 NHS COSI RCT found no added benefit) — so it ships as a **generic, trademark-safe** attachment graphic teaching Bowlby's **secure base** + Ainsworth's **safe haven**, NOT a reproduction of the programme. Nodes placed by `stereotype` (`explore`/`comfort`/`base`); a ring + child dot + cycle arrows + **abstract cradle** (not the CoS hands) + `separate1D` need rows; the disclaimer + alt-text state it is not the trademarked programme. **No schema growth**, edge-free, not `LABEL_IN_BOX`. Named `secure-base` (not `circle-of-security`) on purpose; catalogue #56 maps to it crediting Bowlby/Ainsworth + the IP/evidence caveat. Wired like ADR-0029–0035 (dispatcher + the four test `RENDERERS` maps + `KNOWN_TYPES` + `FAMILY_OF` → **field** + §K compat). Two examples (`secure-base` + golden `showcase-secure-base`); manifest moved #56 `newTypes` → a `diagrams` row, **emptying `newTypes`**; catalog #56 ✅, showcase 20. **All 8 ◇ build-list shapes now ship (ADR-0029–0036); 0 remain** (the BA week-grid #40 stays an out-of-scope worksheet) | `../templates/adr.md`, `0035-decisional-balance-diagram-type.md`, `../../packages/render/index.ts`, `../../examples/secure-base.psyuml` | REQ-NEW-DIAGRAM-TYPES | §E | low (append-only) |
| `0035-decisional-balance-diagram-type.md` | Records the **seventh ◇ renderer** (REQ-NEW-DIAGRAM-TYPES) and a deliberate, transparent **reversal**: a 19th `DiagramType` **`decisional-balance`** + `renderDecisionalBalance` — the MI (Miller–Rollnick) 2×2, **promoted out of the catalogue's "excluded by design" (tables) set**. Re-examined: unlike a flat pros/cons table its crossed **axes** (making-the-change/staying-the-same × benefits/costs) are a load-bearing 2-D structure. Items placed in a quadrant by `stereotype` (`change-benefit`/`stay-benefit`/`change-cost`/`stay-cost`); rendered as a labelled-axis 2×2 (column + row headers, dividing cross, faint per-row tints), items as free haloed labels (no boxes → **not** `LABEL_IN_BOX`). **No schema growth**, edge-free. The **MI ambivalence caveat** (a neutral balance can deepen ambivalence, MI-3) is hard-coded into the footer + alt-text. Honest: the weakest topology of the ◇ set. Wired like ADR-0029–0034 (dispatcher + the four test `RENDERERS` maps + `KNOWN_TYPES` + `FAMILY_OF` → **change** + §K compat). Two examples (`decisional-balance` + golden `showcase-decisional-balance`); manifest moved "Decisional balance" `newTypes` → a verified `diagrams` row; the catalogue's excluded-list drops the entry + records the promotion; catalog #46 ✅, showcase 19. **1 ◇ remains** (Circle of Security) | `../templates/adr.md`, `0034-schema-grid-diagram-type.md`, `../../packages/render/index.ts`, `../../examples/decisional-balance.psyuml` | REQ-NEW-DIAGRAM-TYPES | §E | low (append-only) |
| `0034-schema-grid-diagram-type.md` | Records the **sixth ◇ renderer** (REQ-NEW-DIAGRAM-TYPES): an 18th `DiagramType` **`schema-grid`** + `renderSchemaGrid` — Young's **18 Early Maladaptive Schemas in the 5 schema domains** (sorter/grid). Each node is a **schema** placed in a domain by `stereotype` (5 fixed domain keys, like the Tree-of-Life zones) — **no schema growth**, edge-free; a person's endorsed schema is marked **active** via `properties.intensity` ≥ 0.5 and drawn with a **bold outline + corner wedge + bold label + alt-text** (redundant, never colour alone). A five-**column** sorter; the cells ARE `data-el="node:"` boxes with `wrapLabel`-contained labels, so `schema-grid` joins `layout-quality` `LABEL_IN_BOX` (the first ◇ since the `ladder` to use real boxes). Honest scope: a grid's grouping is the **weakest topology** of the ◇ set — justified by the schema model's own 5-domain structure + the active signal, not spatial meaning. Wired like ADR-0029–0033 (dispatcher + the four test `RENDERERS` maps + `KNOWN_TYPES` + `FAMILY_OF` → **parts** + the `profiles` parts-family test + §K compat + `LABEL_IN_BOX`). Two examples (`schema-grid` + golden `showcase-schema-grid` = the full 18); manifest moved "Schema 18-EMS / 5-domains sorter" `newTypes` → a verified `diagrams` row; catalog #16 ✅, showcase 18. **2 ◇ remain** (decisional-balance 2×2, Circle of Security) | `../templates/adr.md`, `0033-tree-of-life-diagram-type.md`, `../../packages/render/index.ts`, `../../examples/schema-grid.psyuml` | REQ-NEW-DIAGRAM-TYPES | §E | low (append-only) |
| `0033-tree-of-life-diagram-type.md` | Records the **fifth ◇ renderer** (REQ-NEW-DIAGRAM-TYPES) and the **one new layout shape** among the remaining four: a 17th `DiagramType` **`tree-of-life`** + `renderTreeOfLife` — the narrative-therapy **Tree of Life** (Ncube; Dulwich Centre). Each node is an **item** placed in a zone by `stereotype` (`roots`/`ground`/`trunk`/`branches`/`leaves`/`fruits`) — **no schema growth**, **edge-free** (`context` items + `edges: []` lint clean). The renderer buckets nodes by zone, lays each zone as a band of haloed labels spread by `layout.separate1D`, and draws a **canopy + trunk + soil + root-fan silhouette** behind as decoration (no `data-el`); items are the `nodelabel:` content (no owner box — like `venn`/`bullseye`). Wired like ADR-0029–0032 (dispatcher + the four test `RENDERERS` maps + `KNOWN_TYPES` + `FAMILY_OF` → **journey** + §K compat); **not** in `layout-quality` `LABEL_IN_BOX`. Two examples (`tree-of-life` + golden `showcase-tree-of-life`); manifest moved "River / Tree of Life" `newTypes` → a verified `diagrams` row; catalog #33 ✅, showcase 17. **3 ◇ remain** (schema-domains sorter, decisional-balance 2×2, Circle of Security) | `../templates/adr.md`, `0032-bullseye-diagram-type.md`, `../../packages/render/index.ts`, `../../examples/tree-of-life.psyuml` | REQ-NEW-DIAGRAM-TYPES | §E | low (append-only) |
| `0032-bullseye-diagram-type.md` | Records the **fourth ◇ renderer** (REQ-NEW-DIAGRAM-TYPES): a 16th `DiagramType` **`bullseye`** + `renderBullseye` — the ACT (Lundgren) **values bull's-eye** (and the wider "circles of control / influence / concern"). Each node is a life **domain** plotted as a dot whose **radius = `properties.intensity`** (1 = centre / on target, 0 = rim / off target) — **no schema growth** (reuses the field the `ladder` uses for SUDS); the *spread of darts* is the picture. Concentric rings + centre are **decoration (no `data-el`)**; the dot is the `data-el="node:"` and the label rides the **perimeter** (`nodelabel:`, anchored by hemisphere) with a leader — decoupling label placement from the radial data channel keeps labels separated **by construction**. The first **edge-free** corpus model (a bull's-eye plots points, doesn't connect them — only isolated `intervention` nodes are flagged, so `context` domains lint clean with `edges: []`). Wired like ADR-0029/0030/0031 (dispatcher + the four test `RENDERERS` maps + `KNOWN_TYPES` + `FAMILY_OF` → **change** + §K compat); **not** in `layout-quality` `LABEL_IN_BOX` (perimeter labels, no node-box to contain). Two examples (`values-bullseye` + golden `showcase-bullseye`); manifest moved "Values bullseye" `newTypes` → a verified `diagrams` row; catalog #45 ✅, showcase 16. **4 ◇ remain** | `../templates/adr.md`, `0029-ladder-diagram-type.md`, `0031-venn-diagram-type.md`, `../../packages/render/index.ts`, `../../examples/values-bullseye.psyuml` | REQ-NEW-DIAGRAM-TYPES | §E | low (append-only) |
| `0031-venn-diagram-type.md` | Records the **third ◇ renderer** (REQ-NEW-DIAGRAM-TYPES): a 15th `DiagramType` **`venn`** + `renderVenn` — the DBT **states of mind** (Reasonable ∩ Emotion = Wise Mind). Three region nodes by `stereotype` (left / overlap / right); each region's contents via `containment` (the resource-anchor pattern) — **no schema growth**. The two circles are drawn as **decoration, not `data-el` nodes** (they must overlap, which the node↔node invariant forbids); regions are **labels in their zones** (left-only / the lens / right-only) with a haloed one-line contents summary — the first renderer whose nodes are *labels*, not boxes. Wired like ADR-0029/0030 (dispatcher + the four test `RENDERERS` maps + `catalog.test` `KNOWN_TYPES` + `FAMILY_OF` → **parts** + §K compat); **not** in `layout-quality` `LABEL_IN_BOX` (no node boxes), and `index.test`'s showcase check was generalised `data-el="node:"` → `data-el="node(label)?:"` (honest — `venn` nodes are labels). Two examples (`dbt-states-of-mind` + golden `showcase-venn`); manifest moved "DBT states of mind" `newTypes` → a verified `diagrams` row; catalog #20 ✅, showcase 15. **5 ◇ remain** | `../templates/adr.md`, `0029-ladder-diagram-type.md`, `0030-three-circles-diagram-type.md`, `../../packages/render/index.ts`, `../../examples/dbt-states-of-mind.psyuml` | REQ-NEW-DIAGRAM-TYPES | §E | low (append-only) |
| `0030-three-circles-diagram-type.md` | Records the **second ◇ renderer** (REQ-NEW-DIAGRAM-TYPES): a 14th `DiagramType` **`three-circles`** + `renderThreeCircles` — the CFT (Gilbert) threat / drive / soothing model. Systems are nodes by `stereotype`, sized by `properties.weight`; contents via `containment` (the resource-anchor pattern) — **no schema growth**. Drawn in the canonical triangle (soothing at the bottom) so an over-active threat + a depleted soothing system are visible at a glance; name inside (containment-checked), function + contents below; monochrome + alt-text. Wired like ADR-0029 (dispatcher + maps + `FAMILY_OF` → field + §K compat); two examples (`three-circles` + golden `showcase-three-circles`); manifest moved "CFT three circles" `newTypes` → a verified `diagrams` row; catalog #27 ✅. 6 ◇ remain | `../templates/adr.md`, `0029-ladder-diagram-type.md`, `../../packages/render/index.ts`, `../../examples/three-circles.psyuml` | REQ-NEW-DIAGRAM-TYPES | §E | low (append-only) |
| `0029-ladder-diagram-type.md` | Records the **first ◇ new-type renderer** (REQ-NEW-DIAGRAM-TYPES → in-progress): a 13th `DiagramType` **`ladder`** + `renderLadder` — exposure / fear hierarchies + ranked goal ladders. A rung = an ordinary node ranked by `properties.intensity` (0–1 SUDS; **no schema growth**); rendered hardest-first as stacked step-boxes with a ringed rating badge + a worded *harder ↑ / easier* arrow; monochrome + alt-text. Wired into the dispatcher + every test `RENDERERS` map + `catalog.test` `KNOWN_TYPES` + `layout-quality` `LABEL_IN_BOX`. Two examples (`exposure-ladder` + golden-pinned `showcase-ladder`), invariant-clean; the manifest moved "Exposure / fear ladder" from `newTypes` → a verified `diagrams` row, and the generated table + editor gallery picked it up (ADR-0028). 7 ◇ shapes remain | `../templates/adr.md`, `../../packages/model/index.ts`, `../../packages/render/index.ts`, `../../examples/exposure-ladder.psyuml` | REQ-NEW-DIAGRAM-TYPES | §E | low (append-only) |
| `0028-catalog-manifest-single-source.md` | Records making `examples/catalog.json` the **single source** for the shipped example library (closing audit #3 metadata + #5 structure for the shipped set). Each row carries the full schema `{file,type,family,name,school,note,audience∈C\|L\|B}`; **`scripts/build-catalog.mjs` generates** the catalog's per-family "Shipped example library" table from it (drift-checked by `conformance/catalog.test.ts` — committed table parsed back == manifest, content-compared); the **editor gallery derives** its non-showcase items from the same manifest (App.tsx, ~250 hand entries removed; ★ showcase stays a small list); the schema is conformance-enforced. So prose + gallery + corpus can't drift. The hand-authored 59-row family survey tables stay prose (non-shipped ◇ rows). REQ-CATALOG-METADATA → implemented | `../templates/adr.md`, `0027-catalog-corpus-conformance.md`, `../../examples/catalog.json`, `../../scripts/build-catalog.mjs`, `../../apps/web/App.tsx` | REQ-CATALOG-METADATA, REQ-CATALOG-CONFORMANCE, REQ-EXAMPLE-LIBRARY | §E, §L | low (append-only) |
| `0027-catalog-corpus-conformance.md` | Records making the **diagram catalog a verified artifact** (catalog audit dimension #4 was C — the Build plan over-claimed and nothing checked it). A machine-readable manifest **`examples/catalog.json`** + **`conformance/catalog.test.ts`** enforce catalog↔corpus fidelity (every shipped example is real, catalogued, and of its declared renderer type; no orphan; ◇ new-types never shipped) so a "ships now" claim can never again be untrue. **REQ-CATALOG-COVERAGE** shipped **18 new examples** in two batches (CAT pattern; disorder-specific maintenance models incl. PTSD/ED/psychosis/metacognitive/health-anxiety; functional analysis; trauma-timeline; and the relational-field set — empowerment, structural-dissociation, ecomap, social-atom, cultural-genogram, narrative-externalising), each invariant-clean (incl. ADR-0025 edge↔edge — loop exit sourced from the closing node; relational-field laid out as a planar star). 45 non-showcase examples now. REQ-CATALOG-CONFORMANCE + REQ-CATALOG-COVERAGE → implemented | `../templates/adr.md`, `../../docs/research/diagram-catalog-audit.md`, `../../examples/catalog.json`, `../../conformance/catalog.test.ts` | REQ-CATALOG-CONFORMANCE, REQ-CATALOG-COVERAGE, REQ-EXAMPLE-LIBRARY | §E, §J | low (append-only) |
| `0026-editor-visual-identity-case-note.md` | Records the editor **visual-identity redesign** (a `frontend-design` pass) — evolves ADR-0013's aesthetic while keeping its hand-rolled token architecture. A **"clinician's case-note, in ink"** identity: the **◎ notation glyph as the wordmark**, the diagrams' **dashed "held-lightly" stroke** on the honesty `note`, a **formulation-sheet** canvas, a restrained old-style **serif** display face (system stack, zero web-font dep), and **warm paper & ink** neutrals (Okabe–Ito accents kept). The **live diagram is promoted to the hero** (up under the controls). All accessible names/roles/`note` copy preserved (visual pass, not a DOM-semantics change); verified by the 8 e2e journeys. Surfaced + fixed a **stale e2e** assertion (`'Layer'`→`'Audience'`, left from the ADR-0016 rename; e2e isn't in CI) | `../templates/adr.md`, `0013-editor-design-system-responsive.md`, `0016-v0.2-audience-profile-rendering.md`, `../../apps/web/styles.css`, `../../apps/web/App.tsx` | REQ-UX-STORIES, REQ-EDITOR-MVP, REQ-ACCESSIBILITY | §D | low (append-only) |
| `0025-edge-crossings-bounded-and-bridged.md` | Records bringing **edge↔edge crossings into scope** (completing ADR-0012's last honest boundary — they were scoped OUT of the overlap guarantee). Universal planarity is impossible (non-planar clinical graphs; meaningful node order), so the pattern is **measure + bound + make legible**, NOT "guarantee zero": `layout.segSegCross` (proper interior crossing), `introspect.edgeCrossings` (over the `data-el=edge:*` polylines; `pathToSegments` exported), `crossing.test.ts` (count ≤ a pinned per-file `CROSSING_BASELINE`; a NEW gratuitous crossing fails CI), and `addCrossingBridges` (a monochrome metro-map line-hop casing on the over-edge — decorative, no `data-el`, so the invariant still COUNTS the crossing). Census: corpus already near-planar (4/39 cross, all structural — a cross-ring EXIT chord, a cross-map POLARIZATION tie); only those 4 goldens gained bridges. Not a planarity claim — guarantee is bounded + legible | `../templates/adr.md`, `0012-guaranteed-non-overlap-layout.md`, `0021-layout-quality-invariants.md`, `../../packages/render/layout.ts`, `../../packages/render/crossing.test.ts` | REQ-EDGE-CROSSING, REQ-ACCESSIBILITY, REQ-LAYOUT-QUALITY | §D, §2 | low (append-only) |
| `0024-edge-router-interface-two-backends.md` | Records the realized **`EdgeRouter` interface + BOTH backends** (executes ADR-0023; owner cleared LGPL). `router.ts` = the swappable contract (avoid every non-incident node box = edge–vertex resolution > 0; sync `route`, async `init`; `routeToPath`). `BespokeRouter` = dependency-free deterministic orthogonal grid-A* (default); `LibavoidRouter` = LGPL `libavoid-js` WASM (GD'09), dynamic-imported so the wasm loads only if selected, per-edge transaction over non-incident shapes. Both pass ONE contract suite in `router.test.ts` (straight/detour/3-node-band-gap/determinism) via the same `segIntersectsBox` used by the rendered invariant — proving them swappable. `libavoid-js` added to `@psyuml/render`. Remaining: wire a router into the 3 EDGE_NODE_KNOWN_GAP renderers | `../templates/adr.md`, `0023-no-engine-adopt-a-router.md`, `0021-layout-quality-invariants.md`, `../../packages/render/router.ts` | REQ-EDGE-ROUTER, REQ-LAYOUT-QUALITY | §D, §2 | low (append-only) |
| `0023-no-engine-adopt-a-router.md` | Records the **build-vs-adopt decision** (5-angle deep research, `docs/research/diagramming-engine-evaluation.md`): **keep the bespoke deterministic accessible SVG renderer; do NOT adopt a diagramming engine or a layout engine.** Frameworks (Mermaid/GoJS/yFiles/Cytoscape/JointJS) would discard the renderer + regress a11y (Canvas=no DOM/ARIA; Mermaid no relationship semantics) + determinism (DOM/random IDs) + add lock-in/cost; layout engines (ELK/dagre/Graphviz) solve positioning PsyUML already has, not fixed-position routing (ELK itself adopted libavoid for that). Decision: close `REQ-EDGE-ROUTER` with a deterministic edge ROUTER behind a thin `EdgeRouter` interface — bespoke-first (zero-dep/MIT/offline/golden-stable, scoped to the diagram types), LGPL `libavoid-js` WASM as a swappable fallback. Validated against `segIntersectsBox` (ADR-0021). No code | `../templates/adr.md`, `0012-guaranteed-non-overlap-layout.md`, `0021-layout-quality-invariants.md`, `../../docs/research/diagramming-engine-evaluation.md` | REQ-EDGE-ROUTER, REQ-LAYOUT-QUALITY, REQ-ACCESSIBILITY | §D | low (append-only) |
| `0022-gap-analysis-road-to-v1.md` | Records the **gap analysis** ("what's missing — spec these"): a forward spec (`docs/specification/roadmap-to-v1.md`) + **9 `planned` REQs** across phases 8–10 (M19–M21). Decides **v1.0 is defined by the VALIDATION gate, not features** — the comprehension/utility studies (REQ-STUDY-PREREG) are unrun and dominate; expressiveness (◇ renderers, edge router, pictographs) and product depth (case file, live profiles, i18n content, provenance narrative, raster export) sit *behind* it. FHIR import explicitly held out-of-scope. Spec + roadmap only; no code | `../templates/adr.md`, `../../docs/specification/roadmap-to-v1.md`, `../../sdd/traceability.json` | REQ-STUDY-PREREG, REQ-NEW-DIAGRAM-TYPES, REQ-EDGE-ROUTER, REQ-PICTURE-PICTOGRAPHS, REQ-CASE-FILE, REQ-LIVE-PROFILES, REQ-I18N-LOCALIZATION, REQ-PROVENANCE-NARRATIVE, REQ-EXPORT-RASTER | §0–§4 | low (append-only) |
| `0021-layout-quality-invariants.md` | Records **M18**: machine-checked layout-quality invariants closing the two gaps ADR-0012 left open — **(A)** a node's interior text label is contained in its box (`layout.contains`; the existing `fitText`/`wrapLabel` `textLength` compression, now asserted; captions skipped by the centre-outside rule) and **(B)** an edge never crosses a non-incident node (`layout.segIntersectsBox` over `introspect.edgeSegments`; formally edge–vertex resolution > 0). New `introspect.ts` (shared SVG extractor honouring `textLength`), edges tagged `data-el="edge:*"`, ritual edges clipped via `clipToBox`. Honest `EDGE_NODE_KNOWN_GAP` (state-map multi-node bands, ritual cross-phase diagonals, decision-nav back-edges) needs an obstacle-avoiding router. Driven by the cited `docs/research/layout-algorithms.md` survey | `../templates/adr.md`, `0006-rendering-robustness-fit.md`, `0012-guaranteed-non-overlap-layout.md`, `../../docs/research/layout-algorithms.md`, `../../packages/render/layout.ts`, `../../packages/render/introspect.ts` | REQ-LAYOUT-QUALITY, REQ-ACCESSIBILITY, REQ-NOTATION, REQ-CONFORMANCE | §D, §J | low (append-only) |
| `0020-interactive-help-site-example-library.md` | Records **M17**: the interactive help site **is** the editor — no separate app/build. 10 new `examples/*.psyuml` (panic / OCD / vicious-flower / TTM / longitudinal / 5Ps / DBT-chain / goal-ladder / ACT choice-point / staying-well) on top of the 14 existing, all corpus-lint + overlap + legibility clean; a family-grouped `EXAMPLE_CATALOG` carrying each example's school + a one-line **honest** note (evidence/limit) from `diagram-catalog.md`, an "About this example" card + an "About these diagrams — please read" disclosure. Real models in the lint (not hardcoded pictures) → the gallery can't drift; live editing reuses the existing editor. Additive (no schema/Tier-1/new build target) | `../templates/adr.md`, `../../docs/research/diagram-catalog.md`, `../../apps/web/App.tsx`, `../../examples/` | REQ-EXAMPLE-LIBRARY, REQ-EDITOR-MVP, REQ-DIAGRAM-FAMILIES, REQ-AUDIENCE-PROFILES | v0.1 §E, v0.2 §2 | low (append-only) |

## Change checklist
- [ ] Don't rewrite history — supersede with a new ADR and mark the old one `superseded`.
- [ ] Number new ADRs sequentially (`NNNN-short-title.md`).
- [ ] Ran `node sdd/check.mjs` (green).
