# Impact — `packages/render/` (`@psyuml/render`)

**Purpose:** turn a model (under a profile) into SVG, with layers, monochrome, legend, and
alt-text — plus a longitudinal diff "progress card".
**Status:** active (M1–M6 — 11 diagram renderers + the diff card)
**Spec anchor / REQ:** REQ-NOTATION, REQ-ACCESSIBILITY, REQ-EPISTEMIC-STATUS, REQ-LAYOUT-QUALITY, REQ-EDGE-ROUTER

## Upstream (this depends on)
- `@psyuml/model` (what to draw), `@psyuml/diff` (the changeset `renderDiff` visualizes).
- `../../assets/glyphs/` and `../../assets/tokens.json` (the 8 core glyphs + palette).
- `../../docs/specification/psyuml-v0.1.0.md` §B, §C, §D.
- `libavoid-js` (LGPL-2.1 WASM) — used ONLY by `router-libavoid.ts`, via a **dynamic `import()`** so
  the wasm loads only if that backend is selected (the bespoke router is the zero-binary default).

## Downstream (depends on this) — blast radius
> **Blast radius: medium.** `apps/web` renders via this; golden-SVG snapshot tests pin output.

## Files
| File | Purpose | Upstream | Downstream | Spec / REQ | Change risk |
|---|---|---|---|---|---|
| `package.json` | Package manifest (`@psyuml/render`) | — | workspace resolution | — | low |
| `index.ts` | Render API — one `render*` per diagram type: State, Parts, ModeMap, RelationalField (+ drama triangle via `nestedWithin`), BodyMap, Loop, Timeline, InterventionSeq, Ritual, Decision, Resource, TwoTriangles (Malan, via `transference`) — plus `renderDiff(before,after)`. **v0.2 (ADR-0016) `render(model, options)` is the single dispatcher** (by `model.diagram`) that resolves the §2 **audience profile** → label `layer` + `showInterpretive` (clinician shows the analytic surface; client/picture hide it), then delegates; an explicit `layer`/`showInterpretive` still wins, so `layer`-only callers (CLI, goldens) are byte-unchanged. `renderLoopMap` + `renderPartsMap` gate their clinician-analytic surface (contested ⚖, as-if, provenance tags, standing/confidence alt-text) on `showInterpretive`; the descriptive-vs-interpretive dashed border is kept in every profile (§3 MUST). Shared `esc`/`r1`/**`fitText`** (single-line compress) + **`wrapLabel`** (true multi-line `tspan` wrapping, used by the box/shape renderers) + content-fit framing (ADR-0006: Loop fits its viewBox incl. negative coords + honors `pos`; State clamps node centers). **`blankTemplate(model)`** turns a model into a fill-in printable scaffold (REQ-TEMPLATES). Takes `roleLabels` (cross-school vocabulary). The Parts Map draws a node claimed by >1 school as a **contested-origin marker** (`⚖ A vs B`, via `schoolClaims`) + names the conflict in alt-text, surfacing §G.2 disagreement instead of merging it (ADR-0007). The Decision chart's crisis line is region-neutral (ADR-0008). The Decision chart uses a **cycle-aware layered layout** (ADR-0010): `layerWithCycleBreak` DFS-classifies back-edges and longest-path layers the forward edges, so a plan that loops back doesn't collapse the cycle onto one row; the canvas grows to fit the widest layer with a content-fit viewBox, branch labels are offset/wrapped to avoid overprint, and `model.meta.disclaimer` is now rendered into the SVG. The State Map **fans out parallel edges** (ADR-0010) — ≥2 edges between the same pair get distinct lanes + staggered labels (a lone edge is byte-identical). The Relational Field tags each node with a **`data-node-id` hook** so the editor can hit-test it for **drag-to-reposition** (this renderer honors `position`). Eval round 2 (ADR-0011): decision-chart node labels are **wrapped inside their box** (diamonds narrower) so long steps don't overflow into siblings; state-map parallel-edge labels get a **white halo** (`paint-order=stroke`) so band lines/arrows don't strike through; the Parts Map renders the **containment relationship label** (haloed), draws a **conflict tie as a zigzag**, and shifts content down when titled so the title doesn't overprint the top parts. **Guaranteed non-overlap (ADR-0012):** `fitText`/`wrapLabel` measure via the shared `layout.textWidth`; every renderer sizes slots to measured boxes, spreads siblings with `layout.separate1D`, and **grows the canvas / content-fits** rather than overlapping (State Map moves edge labels into left/right gutters outside the node columns; Decision chart separates each layer + spreads branch labels in the inter-row gap; Parts Map grows the orbit radius + canvas; Body Map shrinks-to-fit and flips a crowded label to the freer side). Every logical element is tagged with **`data-el`** (`node:`/`nodelabel:`/`edgelabel:`/`band:`/`banner:`) for the `overlap.test.ts` invariant. **v0.2 Pattern semantics (ADR-0015, §3/§4) on `renderLoopMap`:** a centre **loop-topology marker** (`loopTopologyGlyph` — distinct monochrome trap/dilemma/snag glyph + a redundant uppercase word); interpretive standing (`isInterpretive`) drawn **dashed** (descriptive stays solid); a **`contested` ⚖** marker and an **`as-if`** qualifier carried on the node's own label (owner content — no overlap-invariant impact); and alt-text that echoes topology, contested/interpretive standing, confidence (L/M/H), and as-if framing — so nothing rests on a glyph, border, or colour alone. All additive: a plain v0.1 loop renders byte-identically. **v0.2 §2 Composite board (ADR-0019):** `renderComposite(models)` + `sharedNodeIds()` arrange several member views of one case as titled panels (each via the `render()` dispatcher, nested as a scaled sub-`<svg>`) over a shared-id **cross-navigation index** (ids present in ≥2 views, tagged `data-shared-id` / `data-composite-panel`); takes an **array of models** (a case file), never a model-schema change, never mutates a member. Each panel's `id`/`url(#…)` are **namespaced per panel** (`__pN`) so nested members' shared def ids (`arrow`, the `p-*` band patterns) don't collide (ADR-0019 follow-up). **v0.x layout-quality (ADR-0021):** every edge `<path>`/`<line>` is tagged **`data-el="edge:<id>"`** so the edge↔node invariant can read its geometry; **ritual edges are clipped to the node boxes with `layout.clipToBox`** (arrowheads land on the border, not the centre); the label-in-box guarantee is the existing `fitText`/`wrapLabel` `textLength` compression, now machine-checked by `layout-quality.test.ts`. | model, diff, assets, `layout.ts` | apps/web | §B–§F, §G, §K, §D, v0.2 §3/§4 / REQ-NOTATION, REQ-ACCESSIBILITY, REQ-GENOGRAM, REQ-PROCESS-LOOP, REQ-TIMELINE, REQ-INTERVENTION-SEQ, REQ-RITUAL, REQ-DECISION-NAV, REQ-RESOURCE-ANCHOR, REQ-RESEARCH-PROFILES, REQ-CROSS-SCHOOL, REQ-VERSIONING-DIFF, REQ-PATTERN-SEMANTICS, REQ-PROVENANCE-CONFIDENCE, REQ-AUDIENCE-PROFILES, REQ-COMPOSITE-BOARD, REQ-LAYOUT-QUALITY | medium |
| `index.test.ts` | Unit tests (incl. the content-fit/no-clip frame assertion) | `index.ts` | CI `test` | REQ-ACCESSIBILITY | low |
| `layout.ts` | **The shared geometry layer (ADR-0012)** — the separation/measurement single-source-of-truth that the renderers AND the overlap invariant both import, so geometry is self-consistent. Exports `textWidth` (the one ~0.58·em text metric, matching `fitText`/`wrapLabel`), `Box`/`overlaps`/`union` (AABB intersection with a slop tolerance + content-fit bounding box), and **`separate1D`** — the 1-D core of VPSC / "Fast Node Overlap Removal" (Dwyer–Marriott–Stuckey): shift ordered, sized items minimally so consecutive ones are ≥ gap apart, order preserved, deterministic. Used to spread band/layer/orbit/lane siblings and to stack parallel edge labels. **v0.x layout-quality (ADR-0021):** also exports **`contains`** (AABB-in-AABB, the label-in-box check), **`segIntersectsBox`** (Liang–Barsky segment↔rectangle, the edge↔node check), and **`clipToBox`** (Liang–Barsky entry point — stop an arrow on the target's border). | — | `index.ts`, `introspect.ts`, `overlap.test.ts`, `layout-quality.test.ts` | §D / REQ-ACCESSIBILITY, REQ-NOTATION, REQ-LAYOUT-QUALITY | medium |
| `introspect.ts` | **Shared SVG introspection (ADR-0012/0021)** — reconstructs node/label AABBs (`boxesFromSvg`, honouring `textLength` so compressed labels measure at their rendered width) and **edge polylines** (`edgeSegments`, flattening `M/L/H/V/Q/C`) from `data-el`-tagged elements, with the SAME `layout.textWidth` metric the renderers used. Single source so `overlap.test.ts` and `layout-quality.test.ts` reason about identical geometry | `layout.ts` | `overlap.test.ts`, `layout-quality.test.ts` | §D / REQ-ACCESSIBILITY, REQ-LAYOUT-QUALITY | low |
| `layout.test.ts` | Unit tests for `layout.ts`: the text metric, AABB helpers, `separate1D` over 5 000 random cases (no residual overlap, order preserved, balanced movement), and the layout-quality primitives `contains`/`segIntersectsBox`/`clipToBox` | `layout.ts` | CI `test` | REQ-ACCESSIBILITY, REQ-LAYOUT-QUALITY | low |
| `overlap.test.ts` | **The "nothing ever overlaps" invariant (ADR-0012)** — via `introspect.boxesFromSvg`, asserts for every `examples/*.psyuml` (both layers) + three generated stress models: (1) no node↔node overlap, (2) no label over a non-owner node, (3) no label↔label overlap [enforced for ten of twelve renderers; documented known-gap for parts-map + process-loop], (4) every box in the viewBox. Edge line/path crossings are out of scope HERE — they are the edge↔node invariant in `layout-quality.test.ts` | `index.ts`, `layout.ts`, `introspect.ts`, `../../examples/*.psyuml` | CI `test` | §D, §J / REQ-ACCESSIBILITY, REQ-NOTATION, REQ-CONFORMANCE | low |
| `layout-quality.test.ts` | **The layout-quality invariants (ADR-0021)** — over every `examples/*.psyuml` (both layers): **(A) containment** — every *interior* node label (centre inside its node) fits within the node box (`contains`, a few-px slop for the shared metric's bold/sub-pixel error); enforced for the label-in-box renderers (state/timeline/intervention-sequence/ritual/two-triangles/decision-nav/process-loop), captions drawn beside/below a glyph skipped by the centre-outside rule. **(B) edge↔node** — no edge segment passes through a non-incident node (`segIntersectsBox` over `introspect.edgeSegments`, excluding the edge's own source/target); enforced for the channel/columnar/ring/hand-placed renderers, with a documented `EDGE_NODE_KNOWN_GAP` (state-map multi-node bands, ritual cross-phase diagonals, decision-nav back-edges) that needs an obstacle-avoiding router | `index.ts`, `layout.ts`, `introspect.ts`, `../../examples/*.psyuml` | CI `test` | §D, §J / REQ-LAYOUT-QUALITY, REQ-ACCESSIBILITY, REQ-CONFORMANCE | low |
| `router.ts` | **The `EdgeRouter` interface (ADR-0024, REQ-EDGE-ROUTER)** — the swappable obstacle-avoiding-routing contract: `route(obstacles, edges, options)` → one polyline per edge avoiding every NON-incident node box (edge–vertex resolution > 0); sync `route`, optional async `init`; `DEFAULT_OBSTACLE_MARGIN`, `routeToPath` (polyline → SVG path, tagged `data-el="edge:…"`). Lets a router be added behind an interface (ADR-0023) without touching the renderer | `layout.ts` | `index.ts`, the renderers (future wiring), `router.test.ts` | §D, §2 / REQ-EDGE-ROUTER | low |
| `router-bespoke.ts` | **`BespokeRouter` (default)** — dependency-free, DETERMINISTIC orthogonal grid-A* over the non-incident obstacle boundaries (grown by margin) + endpoints + channel midpoints; endpoints clipped via `clipToBox`; straight when clear. Scoped to PsyUML's diagrams, not a libavoid clone | `layout.ts`, `router.ts` | `index.ts`, `router.test.ts` | §D / REQ-EDGE-ROUTER | medium |
| `router-libavoid.ts` | **`LibavoidRouter` (alternative)** — the reference GD'09 router via the LGPL `libavoid-js` WASM port, behind the same interface; deterministic; `libavoid-js` loaded by a **dynamic `import()` in `init()`** (wasm only if selected); per-edge transaction over non-incident shapes only | `layout.ts`, `router.ts`, `libavoid-js` (dynamic) | `index.ts`, `router.test.ts` | §D / REQ-EDGE-ROUTER | medium |
| `router.test.ts` | **The `EdgeRouter` contract — held by BOTH backends** (`describe.each`): straight-when-clear, detour around a mid node, a reconstructed 3-node-band gap (skip the middle node), and determinism; the avoid-non-incident check reuses `segIntersectsBox` (same geometry as the rendered edge↔node invariant) | `router.ts`, `router-bespoke.ts`, `router-libavoid.ts`, `layout.ts` | CI `test` | §D / REQ-EDGE-ROUTER | low |
| `legibility.test.ts` | **Tier-A legibility + safety dual-coding invariants** (v0.2 §5; motivated, not validated, by the simulated pilots 2–3): every `examples/*.psyuml` rendered in both layers emits **no microtext below an 8px floor**, the State-Map trigger/exit labels are **≥12**, and every `exit` (way-out) edge is **rendered with its word** (never a bare arrow) | `index.ts`, `../../examples/*.psyuml` | CI `test` | §D, v0.2 §5 / REQ-ACCESSIBILITY, REQ-NOTATION-TESTING | low |

## Change checklist
- [ ] Keep color redundant (§D) and emit alt-text/text-summary for every view.
- [ ] Update golden-SVG snapshots intentionally; never let color carry meaning alone.
- [ ] New/changed renderers: tag logical elements with `data-el` (`node:`/`nodelabel:`/`edgelabel:`/`edge:`/`band:`) and keep `overlap.test.ts` + `layout-quality.test.ts` green (size slots via `layout.textWidth`, spread siblings via `separate1D`, grow the canvas rather than overlap; keep interior labels inside their box; clip/route edges so they don't cross non-incident nodes, or add the renderer to the documented `EDGE_NODE_KNOWN_GAP` with a reason).
- [ ] Ran `node sdd/check.mjs` (green).
